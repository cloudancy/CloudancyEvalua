<h1><%= "#{@user.name} #{@user.lastname}" %></h1>


<dl class="dl-horizontal">
  <dt>Email</dt>
  <dd><%= @user.email %></dd>
  <dt>Rol</dt>
  <dd><%= @user.roles.first.name.titleize unless @user.roles.first.nil? %>  
      <a data-toggle="modal" href="#role-options-<%= @user.id %>" class="btn btn-mini" type="button">Cambiar Rol</a>
          <%= render @user %>
  </dd>
  <dt></dt>
  <dd>          
  </dd>
</dl>

<%= link_to 'Editar', edit_user_path %>
